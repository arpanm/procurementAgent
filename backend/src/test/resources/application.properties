# Test-only overrides layered over src/main/resources/application.yml: an isolated in-memory H2
# DB (create-drop per JVM) so the guided-RAG persistence tests never touch the local file DB.
spring.datasource.url=jdbc:h2:mem:procure-test;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop
